services:
  mongodb:
    image: mongo:7
    container_name: pixel-bakery-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: pixel_bakery
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
